export function LoadRegisterButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}
export function FirstRegisterButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}
export function NextRegisterButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}
export function PrevRegisterButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}
export function LastRegisterButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}
export function UpdateRegisterButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}

export function SaveButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}
export function AddRegisterButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}
export function DeleteRegisterButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}
export function ReadRegisterButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}
export function CleanRegisterButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}
export function SortRegisterButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}
export function FilterRegisterButton({ children, onClick, classStyles }) {
  return (
    <>
      <button onClick={onClick} className={classStyles}>
        {children}
      </button>
    </>
  );
}
